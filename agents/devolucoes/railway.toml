[build]
builder = "dockerfile"
dockerfilePath = "agents/devolucoes/Dockerfile"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
